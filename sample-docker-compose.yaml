version: "3.9"

services:

  dynu:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: dynu
    environment:
      - USERNAME=DYNU_USERNAME
      - PASSWORD=DYNU_PASSWORD
      - HOSTNAME=COMMA_SEPARATED_LIST_OF_DOMAINS
      - INTERVAL=UPDATE_INTERVAL_IN_SECONDS
